# Blog Content Policy

本规范用于指导 Agent 判断一段内容是否值得整理成正式 Blog，并进一步分发到公众号、视频或其他平台。

## 1. 什么内容值得写成 Blog

Blog 应该沉淀**有信息增量的认知成果**，而不是简单记录“学到了什么”。

满足以下任意一项，可以考虑 Blog 化：

* **问题解决**：真实遇到的问题、原因分析、解决过程、最终方案。
* **方案设计**：系统设计、架构设计、技术选型，以及为什么这么选。
* **观点讨论**：对技术趋势、产品方向、工程理念形成了明确判断。
* **机制深入**：把一个复杂机制真正讲清楚，并形成自己的理解模型。
* **实验比较**：A/B 方案、性能测试、技术选型、实践验证。
* **复盘总结**：项目完成后的经验、踩坑、错误设计和改进方向。

典型结构应至少包含：

```text
问题 / 背景
    ↓
分析
    ↓
方案或观点
    ↓
取舍
    ↓
结论
```

核心判断：

> 不记录“我知道了什么”，优先记录“我想明白了什么”。

---

## 2. 什么内容通常不值得单独写 Blog

以下内容默认只作为 Notes、Journal 或资料保存：

* 官方文档已经讲得很清楚的基础教程
* 单纯的安装步骤或命令记录
* API、参数、语法的简单罗列
* 没有结论的聊天记录
* 没有实践或观点的信息整理
* 纯粹由 AI 汇总、自己没有消化的内容

例如：

```text
FastAPI 怎么安装
Redis 常用命令
Python List 基础语法
Docker 基础命令大全
今天学习 MCP 的记录
```

除非加入了明显的信息增量，例如：

```text
为什么这个方案会失败
不同方案如何取舍
真实生产环境踩过什么坑
官方文档没有说明的边界条件
自己的实验结果
```

否则不建议正式发布。

---

## 3. Agent 的 Blog 判断流程

Agent 在准备创建 Blog 前，应先判断：

```text
1. 是否存在一个明确的问题、观点或主题？
2. 是否包含作者自己的分析，而不只是资料搬运？
3. 是否存在可复用的结论、方案或经验？
4. 对其他读者是否存在长期价值？
```

满足至少 **2 项**，可以整理为草稿。

满足至少 **3 项**，通常值得正式发布。

如果主要内容仍然是：

```text
零散笔记
聊天记录
命令
资料摘录
未形成判断的想法
```

则不要强行 Blog 化。

优先保留为 Notes，等待后续研究形成完整观点。

---

## 4. Blog 与分发内容的关系

Blog 是内容的 **Source of Truth**。

正式 Blog 完成后，Agent 可以判断是否适合继续分发：

```text
Blog
 │
 ├── 微信公众号：长文传播
 ├── 短视频：核心观点传播
 ├── 长视频：完整解释
 └── 社交平台：观点摘要
```

适合分发的 Blog 通常具有：

* 一个容易理解的问题
* 一个明确观点
* 明显的矛盾或取舍
* 能用一句话总结核心结论
* 不依赖大量代码才能理解

例如：

```text
Agent Runtime 到底应该包含什么？
为什么 Tool 应该渐进式加载？
CLI 云化为什么自然会走向 MCP？
Python 为什么适合控制面，Go 为什么适合数据面？
```

这类内容优先进行多平台分发。

纯代码实现、API 手册、操作步骤类文章，可以保留在 Blog，不必强行视频化或公众号化。

---

## 5. Markdown 标题层级与目录规范

右侧目录（TOC）由标题层级驱动，主题默认只收集 **H2 和 H3**。因此：

```markdown
---
title: ...（frontmatter 提供页面标题）
---

## 1. 大章节      ← H2，出现在右侧目录
### 1.1 小节     ← H3，出现在右侧目录
```

规则：

* **全文只允许一个 H1（`#`），即文章标题**；有 frontmatter `title` 时可直接省略。
* 章节一律从 **H2（`##`）开始**，小节用 H3，再往下用 H4（H4 不进目录，只作为正文结构）。
* 不要整体抬高层级（章节用 `#`、小节用 `##`）——这样大章节会从右侧目录中消失，只有子节可见。
* 手写编号（`1.` / `1.1`）保持与标题在同一行，编号与标题文字之间留一个空格。

---

## 最终原则

Agent 应遵循：

```text
Notes 负责记录
Blog 负责沉淀
公众号负责传播
视频负责放大
```

正式 Blog 必须尽量提供至少一种价值：

```text
解决一个问题
给出一个方案
形成一个判断
讲清一个机制
验证一个结论
总结一段实践
```

如果一篇内容没有明显信息增量：

> 不发布，比为了更新而发布更好。
